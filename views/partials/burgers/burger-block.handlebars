<li>
	{{name}}

	<button class="change-eat" data-id="{{id}}" data-devoured="{{eat}}">
		{{#if eat}}DEVOURED YUM YUM TASTY!{{else}}ID EAT IT!{{/if}}
	</button>

	<button class="delete-cat" data-id="{{this.id}}">DELETE!</button>
</li>
